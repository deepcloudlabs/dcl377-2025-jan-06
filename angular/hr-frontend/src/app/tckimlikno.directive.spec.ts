import { TckimliknoDirective } from './tckimlikno.directive';

describe('TckimliknoDirective', () => {
  it('should create an instance', () => {
    const directive = new TckimliknoDirective();
    expect(directive).toBeTruthy();
  });
});
